<h1>Messages</h1>

<ul>
  <% @messages.each do |message| %>
  <li><%= link_to message.id, message_path(message) %> <%= link_to message.body, message_path(message) %></li>
  <% end %>
</ul>

<%= link_to "New Message", new_message_path, class: "btn btn-info" %>
